{
    "intervalMinutes": 240,
    "xCredentials": {
        "apiKey": "${API_KEY}",
        "apiSecret": "${API_SECRET}",
        "accessToken": "${ACCESS_TOKEN}",
        "accessSecret": "${ACCESS_SECRET}"
    }
}
